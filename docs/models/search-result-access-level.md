# SearchResultAccessLevel

## Example Usage

```typescript
import { SearchResultAccessLevel } from "@pipeshub/sdk/models";

let value: SearchResultAccessLevel = "write";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"read" | "write" | Unrecognized<string>
```