/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { UserRole, UserRole$inboundSchema } from "./user-role.js";

export type Team = {
  /**
   * Unique team identifier
   */
  id?: string | undefined;
  /**
   * Team name
   */
  name: string;
  /**
   * Team description
   */
  description?: string | undefined;
  /**
   * Organization ID
   */
  orgId?: string | undefined;
  /**
   * Users and their roles in the team
   */
  userRoles?: Array<UserRole> | undefined;
  /**
   * Creation timestamp
   */
  createdAt?: number | undefined;
  /**
   * Last update timestamp
   */
  updatedAt?: number | undefined;
};

/** @internal */
export const Team$inboundSchema: z.ZodMiniType<Team, unknown> = z.pipe(
  z.object({
    _id: types.optional(types.string()),
    name: types.string(),
    description: types.optional(types.string()),
    orgId: types.optional(types.string()),
    userRoles: types.optional(z.array(UserRole$inboundSchema)),
    createdAt: types.optional(types.number()),
    updatedAt: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_id": "id",
    });
  }),
);

export function teamFromJSON(
  jsonString: string,
): SafeParseResult<Team, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Team$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Team' from JSON`,
  );
}
