/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Run crawling job once per day at a specified time
 */
export type DailyScheduleConfig = {
  scheduleType: "daily";
  /**
   * Whether the schedule is active. Disabled schedules won't create new jobs.
   */
  isEnabled?: boolean | undefined;
  /**
   * IANA timezone identifier (e.g., "America/New_York", "Europe/London", "Asia/Tokyo")
   */
  timezone?: string | undefined;
  /**
   * Hour of the day to run (0-23, 24-hour format)
   */
  hour: number;
  /**
   * Minute of the hour to run (0-59)
   */
  minute: number;
};

/** @internal */
export const DailyScheduleConfig$inboundSchema: z.ZodMiniType<
  DailyScheduleConfig,
  unknown
> = z.object({
  scheduleType: types.literal("daily"),
  isEnabled: z._default(types.boolean(), true),
  timezone: z._default(types.string(), "UTC"),
  hour: types.number(),
  minute: types.number(),
});
/** @internal */
export type DailyScheduleConfig$Outbound = {
  scheduleType: "daily";
  isEnabled: boolean;
  timezone: string;
  hour: number;
  minute: number;
};

/** @internal */
export const DailyScheduleConfig$outboundSchema: z.ZodMiniType<
  DailyScheduleConfig$Outbound,
  DailyScheduleConfig
> = z.object({
  scheduleType: z.literal("daily"),
  isEnabled: z._default(z.boolean(), true),
  timezone: z._default(z.string(), "UTC"),
  hour: z.int(),
  minute: z.int(),
});

export function dailyScheduleConfigToJSON(
  dailyScheduleConfig: DailyScheduleConfig,
): string {
  return JSON.stringify(
    DailyScheduleConfig$outboundSchema.parse(dailyScheduleConfig),
  );
}
export function dailyScheduleConfigFromJSON(
  jsonString: string,
): SafeParseResult<DailyScheduleConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DailyScheduleConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DailyScheduleConfig' from JSON`,
  );
}
