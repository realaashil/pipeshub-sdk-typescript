/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Run crawling job on specified days of the week
 */
export type WeeklyScheduleConfig = {
  scheduleType: "weekly";
  /**
   * Whether the schedule is active. Disabled schedules won't create new jobs.
   */
  isEnabled?: boolean | undefined;
  /**
   * IANA timezone identifier (e.g., "America/New_York", "Europe/London", "Asia/Tokyo")
   */
  timezone?: string | undefined;
  /**
   * Days of the week to run (0=Sunday, 1=Monday, ..., 6=Saturday).<br>
   *
   * @remarks
   * Example: [1, 3, 5] runs on Monday, Wednesday, Friday
   */
  daysOfWeek: Array<number>;
  /**
   * Hour of the day to run (0-23)
   */
  hour: number;
  /**
   * Minute of the hour to run (0-59)
   */
  minute: number;
};

/** @internal */
export const WeeklyScheduleConfig$inboundSchema: z.ZodMiniType<
  WeeklyScheduleConfig,
  unknown
> = z.object({
  scheduleType: types.literal("weekly"),
  isEnabled: z._default(types.boolean(), true),
  timezone: z._default(types.string(), "UTC"),
  daysOfWeek: z.array(types.number()),
  hour: types.number(),
  minute: types.number(),
});
/** @internal */
export type WeeklyScheduleConfig$Outbound = {
  scheduleType: "weekly";
  isEnabled: boolean;
  timezone: string;
  daysOfWeek: Array<number>;
  hour: number;
  minute: number;
};

/** @internal */
export const WeeklyScheduleConfig$outboundSchema: z.ZodMiniType<
  WeeklyScheduleConfig$Outbound,
  WeeklyScheduleConfig
> = z.object({
  scheduleType: z.literal("weekly"),
  isEnabled: z._default(z.boolean(), true),
  timezone: z._default(z.string(), "UTC"),
  daysOfWeek: z.array(z.int()),
  hour: z.int(),
  minute: z.int(),
});

export function weeklyScheduleConfigToJSON(
  weeklyScheduleConfig: WeeklyScheduleConfig,
): string {
  return JSON.stringify(
    WeeklyScheduleConfig$outboundSchema.parse(weeklyScheduleConfig),
  );
}
export function weeklyScheduleConfigFromJSON(
  jsonString: string,
): SafeParseResult<WeeklyScheduleConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WeeklyScheduleConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WeeklyScheduleConfig' from JSON`,
  );
}
