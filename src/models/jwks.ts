/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";
import { Jwk, Jwk$inboundSchema } from "./jwk.js";

/**
 * JSON Web Key Set (RFC 7517).
 *
 * @remarks
 * Contains public keys for verifying token signatures.
 */
export type Jwks = {
  /**
   * Array of JSON Web Keys
   */
  keys?: Array<Jwk> | undefined;
};

/** @internal */
export const Jwks$inboundSchema: z.ZodMiniType<Jwks, unknown> = z.object({
  keys: types.optional(z.array(Jwk$inboundSchema)),
});

export function jwksFromJSON(
  jsonString: string,
): SafeParseResult<Jwks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Jwks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Jwks' from JSON`,
  );
}
