/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * SAML SSO authentication configuration
 */
export type SSOAuthConfig = {
  /**
   * Identity provider SSO URL
   */
  entryPoint: string;
  /**
   * X.509 certificate for signature validation (PEM format)
   */
  certificate: string;
  /**
   * SAML attribute name for user email
   */
  emailKey: string;
};

/** @internal */
export const SSOAuthConfig$inboundSchema: z.ZodMiniType<
  SSOAuthConfig,
  unknown
> = z.object({
  entryPoint: types.string(),
  certificate: types.string(),
  emailKey: types.string(),
});
/** @internal */
export type SSOAuthConfig$Outbound = {
  entryPoint: string;
  certificate: string;
  emailKey: string;
};

/** @internal */
export const SSOAuthConfig$outboundSchema: z.ZodMiniType<
  SSOAuthConfig$Outbound,
  SSOAuthConfig
> = z.object({
  entryPoint: z.string(),
  certificate: z.string(),
  emailKey: z.string(),
});

export function ssoAuthConfigToJSON(ssoAuthConfig: SSOAuthConfig): string {
  return JSON.stringify(SSOAuthConfig$outboundSchema.parse(ssoAuthConfig));
}
export function ssoAuthConfigFromJSON(
  jsonString: string,
): SafeParseResult<SSOAuthConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SSOAuthConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SSOAuthConfig' from JSON`,
  );
}
