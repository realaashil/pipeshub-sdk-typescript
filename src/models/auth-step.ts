/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AuthMethod,
  AuthMethod$inboundSchema,
  AuthMethod$Outbound,
  AuthMethod$outboundSchema,
} from "./auth-method.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * A single step in multi-factor authentication flow
 */
export type AuthStep = {
  /**
   * Order of the authentication step (1-3, must be unique across steps)
   */
  order: number;
  /**
   * List of allowed authentication methods for this step. User can choose any one method from this list.
   */
  allowedMethods: Array<AuthMethod>;
};

/** @internal */
export const AuthStep$inboundSchema: z.ZodMiniType<AuthStep, unknown> = z
  .object({
    order: types.number(),
    allowedMethods: z.array(AuthMethod$inboundSchema),
  });
/** @internal */
export type AuthStep$Outbound = {
  order: number;
  allowedMethods: Array<AuthMethod$Outbound>;
};

/** @internal */
export const AuthStep$outboundSchema: z.ZodMiniType<
  AuthStep$Outbound,
  AuthStep
> = z.object({
  order: z.int(),
  allowedMethods: z.array(AuthMethod$outboundSchema),
});

export function authStepToJSON(authStep: AuthStep): string {
  return JSON.stringify(AuthStep$outboundSchema.parse(authStep));
}
export function authStepFromJSON(
  jsonString: string,
): SafeParseResult<AuthStep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AuthStep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AuthStep' from JSON`,
  );
}
