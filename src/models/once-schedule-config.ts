/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Run crawling job once at a specific future datetime
 */
export type OnceScheduleConfig = {
  scheduleType: "once";
  /**
   * Whether the schedule is active. Disabled schedules won't create new jobs.
   */
  isEnabled?: boolean | undefined;
  /**
   * IANA timezone identifier (e.g., "America/New_York", "Europe/London", "Asia/Tokyo")
   */
  timezone?: string | undefined;
  /**
   * ISO 8601 datetime when the job should run. Must be in the future.
   */
  scheduledTime: Date;
};

/** @internal */
export const OnceScheduleConfig$inboundSchema: z.ZodMiniType<
  OnceScheduleConfig,
  unknown
> = z.object({
  scheduleType: types.literal("once"),
  isEnabled: z._default(types.boolean(), true),
  timezone: z._default(types.string(), "UTC"),
  scheduledTime: types.date(),
});
/** @internal */
export type OnceScheduleConfig$Outbound = {
  scheduleType: "once";
  isEnabled: boolean;
  timezone: string;
  scheduledTime: string;
};

/** @internal */
export const OnceScheduleConfig$outboundSchema: z.ZodMiniType<
  OnceScheduleConfig$Outbound,
  OnceScheduleConfig
> = z.object({
  scheduleType: z.literal("once"),
  isEnabled: z._default(z.boolean(), true),
  timezone: z._default(z.string(), "UTC"),
  scheduledTime: z.pipe(z.date(), z.transform(v => v.toISOString())),
});

export function onceScheduleConfigToJSON(
  onceScheduleConfig: OnceScheduleConfig,
): string {
  return JSON.stringify(
    OnceScheduleConfig$outboundSchema.parse(onceScheduleConfig),
  );
}
export function onceScheduleConfigFromJSON(
  jsonString: string,
): SafeParseResult<OnceScheduleConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OnceScheduleConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OnceScheduleConfig' from JSON`,
  );
}
