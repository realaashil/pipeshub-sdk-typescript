/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Group type:
 *
 * @remarks
 * - admin: System admin group (cannot be modified)
 * - standard: Default user group
 * - everyone: All users group (cannot be modified)
 * - custom: User-created custom group
 */
export const UserGroupType = {
  Admin: "admin",
  Standard: "standard",
  Everyone: "everyone",
  Custom: "custom",
} as const;
/**
 * Group type:
 *
 * @remarks
 * - admin: System admin group (cannot be modified)
 * - standard: Default user group
 * - everyone: All users group (cannot be modified)
 * - custom: User-created custom group
 */
export type UserGroupType = OpenEnum<typeof UserGroupType>;

/**
 * User group for organizing users within an organization
 */
export type UserGroup = {
  /**
   * Unique group identifier
   */
  id?: string | undefined;
  /**
   * Unique slug for the group
   */
  slug?: string | undefined;
  /**
   * Group type:
   *
   * @remarks
   * - admin: System admin group (cannot be modified)
   * - standard: Default user group
   * - everyone: All users group (cannot be modified)
   * - custom: User-created custom group
   */
  type: UserGroupType;
  /**
   * Group name (unique within organization)
   */
  name: string;
  /**
   * Organization ID
   */
  orgId: string;
  /**
   * Array of user IDs in this group
   */
  users?: Array<string> | undefined;
  /**
   * Soft delete flag
   */
  isDeleted: boolean;
  /**
   * ID of user who deleted this group
   */
  deletedBy?: string | undefined;
  /**
   * Creation timestamp
   */
  createdAt?: number | undefined;
  /**
   * Last update timestamp
   */
  updatedAt?: number | undefined;
};

/** @internal */
export const UserGroupType$inboundSchema: z.ZodMiniType<
  UserGroupType,
  unknown
> = openEnums.inboundSchema(UserGroupType);

/** @internal */
export const UserGroup$inboundSchema: z.ZodMiniType<UserGroup, unknown> = z
  .pipe(
    z.object({
      _id: types.optional(types.string()),
      slug: types.optional(types.string()),
      type: UserGroupType$inboundSchema,
      name: types.string(),
      orgId: types.string(),
      users: types.optional(z.array(types.string())),
      isDeleted: z._default(types.boolean(), false),
      deletedBy: types.optional(types.string()),
      createdAt: types.optional(types.number()),
      updatedAt: types.optional(types.number()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "_id": "id",
      });
    }),
  );

export function userGroupFromJSON(
  jsonString: string,
): SafeParseResult<UserGroup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UserGroup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UserGroup' from JSON`,
  );
}
