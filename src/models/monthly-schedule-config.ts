/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Run crawling job on a specified day of each month
 */
export type MonthlyScheduleConfig = {
  scheduleType: "monthly";
  /**
   * Whether the schedule is active. Disabled schedules won't create new jobs.
   */
  isEnabled?: boolean | undefined;
  /**
   * IANA timezone identifier (e.g., "America/New_York", "Europe/London", "Asia/Tokyo")
   */
  timezone?: string | undefined;
  /**
   * Day of the month to run (1-31). If day doesn't exist in month, job runs on last day.
   */
  dayOfMonth: number;
  /**
   * Hour of the day to run (0-23)
   */
  hour: number;
  /**
   * Minute of the hour to run (0-59)
   */
  minute: number;
};

/** @internal */
export const MonthlyScheduleConfig$inboundSchema: z.ZodMiniType<
  MonthlyScheduleConfig,
  unknown
> = z.object({
  scheduleType: types.literal("monthly"),
  isEnabled: z._default(types.boolean(), true),
  timezone: z._default(types.string(), "UTC"),
  dayOfMonth: types.number(),
  hour: types.number(),
  minute: types.number(),
});
/** @internal */
export type MonthlyScheduleConfig$Outbound = {
  scheduleType: "monthly";
  isEnabled: boolean;
  timezone: string;
  dayOfMonth: number;
  hour: number;
  minute: number;
};

/** @internal */
export const MonthlyScheduleConfig$outboundSchema: z.ZodMiniType<
  MonthlyScheduleConfig$Outbound,
  MonthlyScheduleConfig
> = z.object({
  scheduleType: z.literal("monthly"),
  isEnabled: z._default(z.boolean(), true),
  timezone: z._default(z.string(), "UTC"),
  dayOfMonth: z.int(),
  hour: z.int(),
  minute: z.int(),
});

export function monthlyScheduleConfigToJSON(
  monthlyScheduleConfig: MonthlyScheduleConfig,
): string {
  return JSON.stringify(
    MonthlyScheduleConfig$outboundSchema.parse(monthlyScheduleConfig),
  );
}
export function monthlyScheduleConfigFromJSON(
  jsonString: string,
): SafeParseResult<MonthlyScheduleConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MonthlyScheduleConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MonthlyScheduleConfig' from JSON`,
  );
}
