/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Configuration for application telemetry and metrics collection.
 *
 * @remarks
 * Metrics are collected anonymously and pushed to a remote server for analytics.
 */
export type MetricsCollectionConfig = {
  /**
   * Master switch for metrics collection. When disabled, no metrics are collected or pushed.
   *
   * @remarks
   * Default: true
   */
  enableMetricCollection?: boolean | undefined;
  /**
   * Interval in milliseconds between metrics pushes to the remote server.
   *
   * @remarks
   * Minimum: 1000ms (1 second), Default: 60000ms (1 minute)
   */
  pushIntervalMs: number;
  /**
   * URL of the metrics collection server. For self-hosted analytics, point this
   *
   * @remarks
   * to your own Prometheus-compatible endpoint.
   */
  serverUrl?: string | undefined;
  /**
   * API key for authenticating with the metrics server. Auto-generated if not provided.
   *
   * @remarks
   */
  apiKey?: string | undefined;
  /**
   * Unique identifier for this PipesHub instance. Auto-generated based on hostname.
   *
   * @remarks
   */
  instanceId?: string | undefined;
  /**
   * Current application version for metrics tagging.
   */
  appVersion?: string | undefined;
};

/** @internal */
export const MetricsCollectionConfig$inboundSchema: z.ZodMiniType<
  MetricsCollectionConfig,
  unknown
> = z.object({
  enableMetricCollection: types.optional(types.boolean()),
  pushIntervalMs: z._default(types.number(), 60000),
  serverUrl: types.optional(types.string()),
  apiKey: types.optional(types.string()),
  instanceId: types.optional(types.string()),
  appVersion: types.optional(types.string()),
});

export function metricsCollectionConfigFromJSON(
  jsonString: string,
): SafeParseResult<MetricsCollectionConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricsCollectionConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricsCollectionConfig' from JSON`,
  );
}
