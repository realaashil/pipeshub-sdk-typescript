/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * SMTP email server configuration for sending system emails (invitations, notifications, password resets)
 */
export type SMTPConfig = {
  /**
   * SMTP server hostname or IP address
   */
  host: string;
  /**
   * SMTP server port. Common ports are 25 (unencrypted), 465 (SSL), 587 (TLS/STARTTLS)
   */
  port: number;
  /**
   * SMTP authentication username. Usually an email address for services like Gmail, SendGrid, etc.
   */
  username?: string | undefined;
  /**
   * SMTP authentication password or app-specific password. For Gmail, use an App Password instead of your account password.
   */
  password?: string | undefined;
  /**
   * Default sender email address that appears in the "From" field of outgoing emails
   */
  fromEmail: string;
};

/** @internal */
export const SMTPConfig$inboundSchema: z.ZodMiniType<SMTPConfig, unknown> = z
  .object({
    host: types.string(),
    port: types.number(),
    username: types.optional(types.string()),
    password: types.optional(types.string()),
    fromEmail: types.string(),
  });
/** @internal */
export type SMTPConfig$Outbound = {
  host: string;
  port: number;
  username?: string | undefined;
  password?: string | undefined;
  fromEmail: string;
};

/** @internal */
export const SMTPConfig$outboundSchema: z.ZodMiniType<
  SMTPConfig$Outbound,
  SMTPConfig
> = z.object({
  host: z.string(),
  port: z.int(),
  username: z.optional(z.string()),
  password: z.optional(z.string()),
  fromEmail: z.string(),
});

export function smtpConfigToJSON(smtpConfig: SMTPConfig): string {
  return JSON.stringify(SMTPConfig$outboundSchema.parse(smtpConfig));
}
export function smtpConfigFromJSON(
  jsonString: string,
): SafeParseResult<SMTPConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SMTPConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SMTPConfig' from JSON`,
  );
}
