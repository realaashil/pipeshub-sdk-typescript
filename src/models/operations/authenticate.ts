/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";

export type AuthenticateRequest = {
  /**
   * Session token received from `/initAuth` endpoint
   */
  xSessionToken: string;
  /**
   * Request payload
   */
  body: models.AuthenticateRequest;
};

/** @internal */
export type AuthenticateRequest$Outbound = {
  "x-session-token": string;
  body: models.AuthenticateRequest$Outbound;
};

/** @internal */
export const AuthenticateRequest$outboundSchema: z.ZodMiniType<
  AuthenticateRequest$Outbound,
  AuthenticateRequest
> = z.pipe(
  z.object({
    xSessionToken: z.string(),
    body: models.AuthenticateRequest$outboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      xSessionToken: "x-session-token",
    });
  }),
);

export function authenticateRequestToJSON(
  authenticateRequest: AuthenticateRequest,
): string {
  return JSON.stringify(
    AuthenticateRequest$outboundSchema.parse(authenticateRequest),
  );
}
