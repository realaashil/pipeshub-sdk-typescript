/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export const DoclingServiceHealthServerList = [
  /**
   * Docling Service (Internal)
   */
  "http://localhost:8081",
] as const;

export const DoclingServiceHealthStatus = {
  Healthy: "healthy",
} as const;
export type DoclingServiceHealthStatus = ClosedEnum<
  typeof DoclingServiceHealthStatus
>;

/**
 * Service is healthy
 */
export type DoclingServiceHealthResponse = {
  status?: DoclingServiceHealthStatus | undefined;
  service?: string | undefined;
};

/** @internal */
export const DoclingServiceHealthStatus$inboundSchema: z.ZodMiniEnum<
  typeof DoclingServiceHealthStatus
> = z.enum(DoclingServiceHealthStatus);

/** @internal */
export const DoclingServiceHealthResponse$inboundSchema: z.ZodMiniType<
  DoclingServiceHealthResponse,
  unknown
> = z.object({
  status: types.optional(DoclingServiceHealthStatus$inboundSchema),
  service: types.optional(types.string()),
});

export function doclingServiceHealthResponseFromJSON(
  jsonString: string,
): SafeParseResult<DoclingServiceHealthResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DoclingServiceHealthResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DoclingServiceHealthResponse' from JSON`,
  );
}
