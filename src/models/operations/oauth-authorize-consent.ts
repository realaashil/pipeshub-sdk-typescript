/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Consent processed, returns redirect URL with authorization code
 */
export type OauthAuthorizeConsentResponse = {
  /**
   * URL to redirect user (includes code and state)
   */
  redirectUri?: string | undefined;
};

/** @internal */
export const OauthAuthorizeConsentResponse$inboundSchema: z.ZodMiniType<
  OauthAuthorizeConsentResponse,
  unknown
> = z.pipe(
  z.object({
    redirect_uri: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "redirect_uri": "redirectUri",
    });
  }),
);

export function oauthAuthorizeConsentResponseFromJSON(
  jsonString: string,
): SafeParseResult<OauthAuthorizeConsentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OauthAuthorizeConsentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OauthAuthorizeConsentResponse' from JSON`,
  );
}
