/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * User's role in this knowledge base
 */
export const UserRoleEnum = {
  Owner: "OWNER",
  Organizer: "ORGANIZER",
  Fileorganizer: "FILEORGANIZER",
  Writer: "WRITER",
  Commenter: "COMMENTER",
  Reader: "READER",
} as const;
/**
 * User's role in this knowledge base
 */
export type UserRoleEnum = OpenEnum<typeof UserRoleEnum>;

export type KnowledgeBase = {
  /**
   * Unique knowledge base identifier
   */
  key?: string | undefined;
  /**
   * Name of the knowledge base
   */
  name: string;
  /**
   * Organization ID
   */
  orgId: string;
  /**
   * Creation timestamp
   */
  createdAtTimestamp?: number | undefined;
  /**
   * Last update timestamp
   */
  updatedAtTimestamp?: number | undefined;
  /**
   * User's role in this knowledge base
   */
  userRole?: UserRoleEnum | undefined;
  /**
   * Whether the knowledge base is deleted
   */
  isDeleted: boolean;
};

/** @internal */
export const UserRoleEnum$inboundSchema: z.ZodMiniType<UserRoleEnum, unknown> =
  openEnums.inboundSchema(UserRoleEnum);

/** @internal */
export const KnowledgeBase$inboundSchema: z.ZodMiniType<
  KnowledgeBase,
  unknown
> = z.pipe(
  z.object({
    _key: types.optional(types.string()),
    name: types.string(),
    orgId: types.string(),
    createdAtTimestamp: types.optional(types.number()),
    updatedAtTimestamp: types.optional(types.number()),
    userRole: types.optional(UserRoleEnum$inboundSchema),
    isDeleted: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "_key": "key",
    });
  }),
);

export function knowledgeBaseFromJSON(
  jsonString: string,
): SafeParseResult<KnowledgeBase, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => KnowledgeBase$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'KnowledgeBase' from JSON`,
  );
}
