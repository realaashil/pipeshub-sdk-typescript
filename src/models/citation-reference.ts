/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Reference to a source document cited in a response
 */
export type CitationReference = {
  /**
   * ID of the citation record
   */
  citationId?: string | undefined;
  /**
   * How relevant this citation is to the query (0-1)
   */
  relevanceScore?: number | undefined;
  /**
   * Relevant excerpt from the source document
   */
  excerpt?: string | undefined;
  /**
   * Additional context around the citation
   */
  context?: string | undefined;
};

/** @internal */
export const CitationReference$inboundSchema: z.ZodMiniType<
  CitationReference,
  unknown
> = z.object({
  citationId: types.optional(types.string()),
  relevanceScore: types.optional(types.number()),
  excerpt: types.optional(types.string()),
  context: types.optional(types.string()),
});

export function citationReferenceFromJSON(
  jsonString: string,
): SafeParseResult<CitationReference, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CitationReference$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CitationReference' from JSON`,
  );
}
