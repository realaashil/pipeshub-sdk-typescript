/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

/**
 * Run crawling job every X hours at a specified minute
 */
export type HourlyScheduleConfig = {
  scheduleType: "hourly";
  /**
   * Whether the schedule is active. Disabled schedules won't create new jobs.
   */
  isEnabled?: boolean | undefined;
  /**
   * IANA timezone identifier (e.g., "America/New_York", "Europe/London", "Asia/Tokyo")
   */
  timezone?: string | undefined;
  /**
   * Minute of the hour to run (0-59)
   */
  minute: number;
  /**
   * Run every X hours (1 = every hour, 2 = every 2 hours, etc.)
   */
  interval?: number | undefined;
};

/** @internal */
export const HourlyScheduleConfig$inboundSchema: z.ZodMiniType<
  HourlyScheduleConfig,
  unknown
> = z.object({
  scheduleType: types.literal("hourly"),
  isEnabled: z._default(types.boolean(), true),
  timezone: z._default(types.string(), "UTC"),
  minute: types.number(),
  interval: z._default(types.number(), 1),
});
/** @internal */
export type HourlyScheduleConfig$Outbound = {
  scheduleType: "hourly";
  isEnabled: boolean;
  timezone: string;
  minute: number;
  interval: number;
};

/** @internal */
export const HourlyScheduleConfig$outboundSchema: z.ZodMiniType<
  HourlyScheduleConfig$Outbound,
  HourlyScheduleConfig
> = z.object({
  scheduleType: z.literal("hourly"),
  isEnabled: z._default(z.boolean(), true),
  timezone: z._default(z.string(), "UTC"),
  minute: z.int(),
  interval: z._default(z.int(), 1),
});

export function hourlyScheduleConfigToJSON(
  hourlyScheduleConfig: HourlyScheduleConfig,
): string {
  return JSON.stringify(
    HourlyScheduleConfig$outboundSchema.parse(hourlyScheduleConfig),
  );
}
export function hourlyScheduleConfigFromJSON(
  jsonString: string,
): SafeParseResult<HourlyScheduleConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HourlyScheduleConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HourlyScheduleConfig' from JSON`,
  );
}
