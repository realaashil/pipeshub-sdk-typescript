/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { DownloadDocumentResponseBody } from "../models/operations/index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Document Management Download Document", async () => {
  const testHttpClient = createTestHTTPClient("downloadDocument");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.documentManagement.downloadDocument({
    documentId: "507f1f77bcf86cd799439011",
    version: 2,
    expirationTimeInSeconds: 7200,
  });
  expect(result).toBeDefined();
  expect(result as DownloadDocumentResponseBody).toEqual({
    success: true,
    signedUrl:
      "https://bucket.s3.amazonaws.com/docs/file.pdf?X-Amz-Signature=...",
  });
});
