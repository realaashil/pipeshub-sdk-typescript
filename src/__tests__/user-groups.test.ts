/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("User Groups Create User Group", async () => {
  const testHttpClient = createTestHTTPClient("createUserGroup");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.createUserGroup({
    name: "Engineering Team",
    type: "standard",
    description: "All engineering department members",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    type: "standard",
    name: "<value>",
    orgId: "<value>",
    isDeleted: false,
  });
});

test("User Groups Get All User Groups", async () => {
  const testHttpClient = createTestHTTPClient("getAllUserGroups");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.getAllUserGroups();
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
  });
});

test("User Groups Get User Group By Id", async () => {
  const testHttpClient = createTestHTTPClient("getUserGroupById");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.getUserGroupById({
    groupId: "507f1f77bcf86cd799439011",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    type: "custom",
    name: "<value>",
    orgId: "<value>",
    isDeleted: false,
  });
});

test("User Groups Update User Group", async () => {
  const testHttpClient = createTestHTTPClient("updateUserGroup");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.updateUserGroup({
    groupId: "507f1f77bcf86cd799439011",
    body: {
      name: "Engineering Team - Updated",
      description: "All engineering and DevOps members",
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    type: "admin",
    name: "<value>",
    orgId: "<value>",
    isDeleted: false,
  });
});

test("User Groups Delete User Group", async () => {
  const testHttpClient = createTestHTTPClient("deleteUserGroup");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.deleteUserGroup({
    groupId: "507f1f77bcf86cd799439011",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "User group deleted successfully",
  });
});

test("User Groups Add Users To Group", async () => {
  const testHttpClient = createTestHTTPClient("addUsersToGroup");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.addUsersToGroup({
    groupId: "507f1f77bcf86cd799439011",
    userIds: [
      "507f1f77bcf86cd799439012",
      "507f1f77bcf86cd799439013",
    ],
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "2 users added to group successfully",
    addedCount: 2,
    skippedCount: 0,
  });
});

test("User Groups Remove Users From Group", async () => {
  const testHttpClient = createTestHTTPClient("removeUsersFromGroup");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.removeUsersFromGroup({
    groupId: "507f1f77bcf86cd799439011",
    userIds: [
      "507f1f77bcf86cd799439012",
    ],
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "1 user removed from group successfully",
    removedCount: 1,
  });
});

test("User Groups Get Groups For User", async () => {
  const testHttpClient = createTestHTTPClient("getGroupsForUser");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.userGroups.getGroupsForUser({
    userId: "507f1f77bcf86cd799439012",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
  });
});
