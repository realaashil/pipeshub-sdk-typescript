/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Agents List Agents", async () => {
  const testHttpClient = createTestHTTPClient("listAgents");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.listAgents();
  expect(result).toBeDefined();
  expect(result).toEqual([
    {
      agentKey: "customer-support-agent",
      name: "Customer Support Assistant",
    },
  ]);
});

test("Agents Create Agent", async () => {
  const testHttpClient = createTestHTTPClient("createAgent");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.createAgent({
    name: "Product Support Agent",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    agentKey: "customer-support-agent",
    name: "Customer Support Assistant",
  });
});

test("Agents List Agent Tools", async () => {
  const testHttpClient = createTestHTTPClient("listAgentTools");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.listAgentTools();
  expect(result).toBeDefined();
  expect(result).toEqual([
    {
      key: "web-search",
      name: "Web Search",
    },
  ]);
});

test("Agents Get Agent", async () => {
  const testHttpClient = createTestHTTPClient("getAgent");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.getAgent({
    agentKey: "customer-support-agent",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    agentKey: "customer-support-agent",
    name: "Customer Support Assistant",
  });
});

test("Agents Update Agent", async () => {
  const testHttpClient = createTestHTTPClient("updateAgent");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.updateAgent({
    agentKey: "<value>",
    body: {},
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    agentKey: "customer-support-agent",
    name: "Customer Support Assistant",
  });
});

test("Agents Get Agent Permissions", async () => {
  const testHttpClient = createTestHTTPClient("getAgentPermissions");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.getAgentPermissions({
    agentKey: "<value>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});

test("Agents Share Agent", async () => {
  const testHttpClient = createTestHTTPClient("shareAgent");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agents.shareAgent({
    agentKey: "<value>",
    body: {
      userIds: [
        "507f1f77bcf86cd799439011",
      ],
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    agentKey: "customer-support-agent",
    name: "Customer Support Assistant",
  });
});
