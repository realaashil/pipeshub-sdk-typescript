/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Metrics Collection Get Metrics Collection", async () => {
  const testHttpClient = createTestHTTPClient("getMetricsCollection");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.metricsCollection.getMetricsCollection();
  expect(result).toBeDefined();
  expect(result).toEqual({
    enableMetricCollection: "true",
    pushIntervalMs: "60000",
    serverUrl: "https://metrics-collector.example.com/collect-metrics",
    apiKey: "54523f794e23d96e",
    appVersion: "1.0.0",
  });
});

test("Metrics Collection Toggle Metrics Collection Enable", async () => {
  const testHttpClient = createTestHTTPClient("toggleMetricsCollection-enable");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.metricsCollection.toggleMetricsCollection({
    enableMetricCollection: true,
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Metrics collection toggled successfully",
  });
});

test("Metrics Collection Toggle Metrics Collection Disable", async () => {
  const testHttpClient = createTestHTTPClient(
    "toggleMetricsCollection-disable",
  );

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.metricsCollection.toggleMetricsCollection({
    enableMetricCollection: false,
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Metrics collection toggled successfully",
  });
});

test("Metrics Collection Toggle Metrics Collection", async () => {
  const testHttpClient = createTestHTTPClient("toggleMetricsCollection");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.metricsCollection.toggleMetricsCollection({
    enableMetricCollection: true,
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Metrics collection toggled successfully",
  });
});
