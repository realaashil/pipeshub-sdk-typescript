/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Teams Create Team", async () => {
  const testHttpClient = createTestHTTPClient("createTeam");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.createTeam({
    name: "Engineering Team",
    description: "Core engineering team for product development",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "Team created successfully",
  });
});

test("Teams List Teams", async () => {
  const testHttpClient = createTestHTTPClient("listTeams");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.listTeams({
    search: "engineering",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
  });
});

test("Teams Get Team By Id", async () => {
  const testHttpClient = createTestHTTPClient("getTeamById");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.getTeamById({
    teamId: "507f1f77bcf86cd799439011",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
  });
});

test("Teams Update Team", async () => {
  const testHttpClient = createTestHTTPClient("updateTeam");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.updateTeam({
    teamId: "507f1f77bcf86cd799439011",
    body: {
      name: "Core Engineering Team",
      description: "Primary engineering team for product development",
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "Team updated successfully",
  });
});

test("Teams Delete Team", async () => {
  const testHttpClient = createTestHTTPClient("deleteTeam");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.deleteTeam({
    teamId: "507f1f77bcf86cd799439011",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    message: "Team deleted successfully",
  });
});

test("Teams Get User Teams", async () => {
  const testHttpClient = createTestHTTPClient("getUserTeams");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.teams.getUserTeams({});
  expect(result).toBeDefined();
  expect(result).toEqual({
    success: true,
    pagination: {
      page: 1,
      limit: 20,
      total: 5,
    },
  });
});
