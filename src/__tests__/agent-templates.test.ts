/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Agent Templates List Agent Templates", async () => {
  const testHttpClient = createTestHTTPClient("listAgentTemplates");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agentTemplates.listAgentTemplates();
  expect(result).toBeDefined();
  expect(result).toEqual([
    {
      name: "Technical Support Template",
      category: "Customer Service",
    },
  ]);
});

test("Agent Templates Create Agent Template", async () => {
  const testHttpClient = createTestHTTPClient("createAgentTemplate");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agentTemplates.createAgentTemplate({
    name: "Customer Support Agent",
    category: "Support",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    name: "Technical Support Template",
    category: "Customer Service",
  });
});

test("Agent Templates Get Agent Template", async () => {
  const testHttpClient = createTestHTTPClient("getAgentTemplate");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agentTemplates.getAgentTemplate({
    templateId: "<id>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    name: "Technical Support Template",
    category: "Customer Service",
  });
});

test("Agent Templates Update Agent Template", async () => {
  const testHttpClient = createTestHTTPClient("updateAgentTemplate");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.agentTemplates.updateAgentTemplate({
    templateId: "<id>",
    body: {},
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    name: "Technical Support Template",
    category: "Customer Service",
  });
});
