/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Connector Registry Get Connector Registry", async () => {
  const testHttpClient = createTestHTTPClient("getConnectorRegistry");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorRegistry.getConnectorRegistry({
    scope: "team",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    connectors: [
      {
        connectorType: "google-drive",
        displayName: "Google Drive",
        supportedScopes: [
          "team",
        ],
      },
    ],
  });
});

test("Connector Registry Get Connector Schema", async () => {
  const testHttpClient = createTestHTTPClient("getConnectorSchema");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorRegistry.getConnectorSchema({
    connectorType: "google-drive",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});
