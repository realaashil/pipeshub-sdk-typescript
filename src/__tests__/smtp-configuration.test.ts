/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Smtp Configuration Get Smtp Config", async () => {
  const testHttpClient = createTestHTTPClient("getSMTPConfig");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.smtpConfiguration.getSMTPConfig();
  expect(result).toBeDefined();
  expect(result).toEqual({
    host: "smtp.gmail.com",
    port: 587,
    username: "notifications@yourcompany.com",
    password: "your-app-password",
    fromEmail: "noreply@yourcompany.com",
  });
});
