/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Organization Auth Config Get Auth Methods", async () => {
  const testHttpClient = createTestHTTPClient("getAuthMethods");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.organizationAuthConfig.getAuthMethods();
  expect(result).toBeDefined();
  expect(result).toEqual({
    authMethods: [
      {
        order: 216367,
        allowedMethods: [
          {
            type: "azureAd",
          },
        ],
      },
    ],
  });
});

test("Organization Auth Config Update Auth Method", async () => {
  const testHttpClient = createTestHTTPClient("updateAuthMethod");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.organizationAuthConfig.updateAuthMethod({
    authMethods: [
      {
        order: 195644,
        allowedMethods: [
          {
            type: "samlSso",
          },
        ],
      },
    ],
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Authentication methods updated",
  });
});
