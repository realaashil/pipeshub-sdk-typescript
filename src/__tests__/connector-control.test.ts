/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Connector Control Toggle Connector", async () => {
  const testHttpClient = createTestHTTPClient("toggleConnector");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorControl.toggleConnector({
    connectorId: "<id>",
    body: {
      type: "sync",
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Sync enabled successfully",
    connector: {
      connectorId: "conn_abc123",
      connectorType: "google-drive",
      instanceName: "Company Google Drive",
      scope: "team",
    },
  });
});
