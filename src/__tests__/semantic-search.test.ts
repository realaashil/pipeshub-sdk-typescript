/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Semantic Search Search Simple", async () => {
  const testHttpClient = createTestHTTPClient("search-simple");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.semanticSearch.search({
    query: "company vacation policy",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    isShared: false,
    isArchived: false,
  });
});

test("Semantic Search Search Filtered", async () => {
  const testHttpClient = createTestHTTPClient("search-filtered");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.semanticSearch.search({
    query: "API documentation examples",
    filters: {
      apps: [
        "drive",
      ],
    },
    limit: 20,
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    isShared: false,
    isArchived: false,
  });
});

test("Semantic Search Search", async () => {
  const testHttpClient = createTestHTTPClient("search");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.semanticSearch.search({
    query: "employee onboarding procedures",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    isShared: false,
    isArchived: false,
  });
});

test("Semantic Search Search History", async () => {
  const testHttpClient = createTestHTTPClient("searchHistory");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.semanticSearch.searchHistory({});
  expect(result).toBeDefined();
  expect(result).toEqual({});
});

test("Semantic Search Delete All Search History", async () => {
  const testHttpClient = createTestHTTPClient("deleteAllSearchHistory");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.semanticSearch.deleteAllSearchHistory();
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Search history cleared successfully",
  });
});
