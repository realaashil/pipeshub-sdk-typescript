/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Oauth Configuration Get O Auth Registry", async () => {
  const testHttpClient = createTestHTTPClient("getOAuthRegistry");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.getOAuthRegistry({});
  expect(result).toBeDefined();
  expect(result).toEqual({
    connectors: [
      {
        connectorType: "google-drive",
        displayName: "Google Drive",
        supportedScopes: [
          "team",
        ],
      },
    ],
  });
});

test("Oauth Configuration Get O Auth Connector Type", async () => {
  const testHttpClient = createTestHTTPClient("getOAuthConnectorType");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.getOAuthConnectorType({
    connectorType: "<value>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    connector: {
      connectorType: "google-drive",
      displayName: "Google Drive",
      supportedScopes: [
        "team",
      ],
    },
  });
});

test("Oauth Configuration List O Auth Configs", async () => {
  const testHttpClient = createTestHTTPClient("listOAuthConfigs");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.listOAuthConfigs({});
  expect(result).toBeDefined();
  expect(result).toEqual({
    oauthConfigs: [
      {
        oauthInstanceName: "Production Google OAuth",
      },
    ],
  });
});

test("Oauth Configuration List O Auth Configs By Type", async () => {
  const testHttpClient = createTestHTTPClient("listOAuthConfigsByType");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.listOAuthConfigsByType({
    connectorType: "<value>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    oauthConfigs: [
      {
        oauthInstanceName: "Production Google OAuth",
      },
    ],
  });
});

test("Oauth Configuration Create O Auth Config", async () => {
  const testHttpClient = createTestHTTPClient("createOAuthConfig");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.createOAuthConfig({
    connectorType: "<value>",
    body: {
      oauthInstanceName: "Production Google OAuth Credentials",
      config: {
        clientId: "123456789-abc.apps.googleusercontent.com",
        clientSecret: "GOCSPX-xxxxxxxxxxxxx",
        tenantId: "12345678-1234-1234-1234-123456789abc",
      },
      baseUrl: "https://gitlab.mycompany.com",
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    oauthConfig: {
      oauthInstanceName: "Production Google OAuth",
    },
  });
});

test("Oauth Configuration Get O Auth Config", async () => {
  const testHttpClient = createTestHTTPClient("getOAuthConfig");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.getOAuthConfig({
    connectorType: "<value>",
    configId: "<id>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    oauthConfig: {
      oauthInstanceName: "Production Google OAuth",
    },
  });
});

test("Oauth Configuration Update O Auth Config", async () => {
  const testHttpClient = createTestHTTPClient("updateOAuthConfig");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.updateOAuthConfig({
    connectorType: "<value>",
    configId: "<id>",
    body: {},
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    oauthConfig: {
      oauthInstanceName: "Production Google OAuth",
    },
  });
});

test("Oauth Configuration Delete O Auth Config", async () => {
  const testHttpClient = createTestHTTPClient("deleteOAuthConfig");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.oAuthConfiguration.deleteOAuthConfig({
    connectorType: "<value>",
    configId: "<id>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});
