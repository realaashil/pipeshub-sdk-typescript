/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { createTestHTTPClient } from "./test-client.js";

test("Connector Filters Get Connector Filters", async () => {
  const testHttpClient = createTestHTTPClient("getConnectorFilters");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorFilters.getConnectorFilters({
    connectorId: "<id>",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});

test("Connector Filters Save Connector Filters", async () => {
  const testHttpClient = createTestHTTPClient("saveConnectorFilters");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorFilters.saveConnectorFilters({
    connectorId: "<id>",
    body: {
      filters: {
        "fileTypes": [
          "pdf",
          "docx",
        ],
        "folders": [
          {
            "id": "folder_123",
            "name": "Documents",
          },
          {
            "id": "folder_456",
            "name": "Reports",
          },
        ],
        "modifiedAfter": "2024-01-01",
      },
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});

test("Connector Filters Get Filter Field Options", async () => {
  const testHttpClient = createTestHTTPClient("getFilterFieldOptions");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.connectorFilters.getFilterFieldOptions({
    connectorId: "<id>",
    filterKey: "folders",
  });
  expect(result).toBeDefined();
  expect(result).toEqual({});
});
