/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { expect, test } from "vitest";
import { Pipeshub } from "../index.js";
import { filesToStream } from "./files.js";
import { createTestHTTPClient } from "./test-client.js";

test("Upload Upload Records To Kb", async () => {
  const testHttpClient = createTestHTTPClient("uploadRecordsToKB");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.upload.uploadRecordsToKB({
    kbId: "<id>",
    body: {
      files: [
        {
          fileName: "example.file",
          content: filesToStream(".speakeasy/testfiles/example.file"),
        },
      ],
      filesMetadata:
        "[{\"file_path\":\"/docs/report.pdf\",\"last_modified\":\"2024-01-15T10:30:00Z\"}]",
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Files uploaded successfully",
  });
});

test("Upload Upload Records To Folder", async () => {
  const testHttpClient = createTestHTTPClient("uploadRecordsToFolder");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.upload.uploadRecordsToFolder({
    kbId: "<id>",
    folderId: "<id>",
    body: {
      files: [
        {
          fileName: "example.file",
          content: filesToStream(".speakeasy/testfiles/example.file"),
        },
      ],
    },
  });
  expect(result).toBeDefined();
  expect(result).toEqual({
    message: "Files uploaded successfully",
  });
});

test("Upload Get Upload Limits", async () => {
  const testHttpClient = createTestHTTPClient("getUploadLimits");

  const pipeshub = new Pipeshub({
    serverURL: process.env["TEST_SERVER_URL"] ?? "http://localhost:18080",
    httpClient: testHttpClient,
    security: {
      bearerAuth: "<YOUR_BEARER_TOKEN_HERE>",
    },
  });

  const result = await pipeshub.upload.getUploadLimits();
  expect(result).toBeDefined();
  expect(result).toEqual({
    maxFilesPerRequest: 1000,
    maxFileSizeBytes: 31457280,
  });
});
